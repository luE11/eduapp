spring:
  jpa:
    hibernate:
      #ddl-auto: none
      #ddl-auto: update
    show-sql: true
  # MySQL
  datasource:
    url: jdbc:mysql://localhost:3306/eduapp-test
    username: root
  sql:
    init:
      data-locations:
        - classpath:test-data.sql
      mode: ALWAYS

jwt:
  secret: jwt_frase_secreta_xd